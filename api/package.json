{
  "name": "leafstudio-activation-api",
  "version": "1.0.0",
  "description": "LeafStudio Activation API - Cloudflare Worker",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy worker.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  }
}