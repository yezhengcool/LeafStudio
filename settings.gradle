pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/jcenter' }
        maven { url 'https://download.videolan.org/pub/Maven' } // VideoLAN 官方 Maven 仓库
        jcenter() // Deprecated but may have IJKPlayer cached
    }
}

rootProject.name = "LeafStudio TV Player"
include ':app'
