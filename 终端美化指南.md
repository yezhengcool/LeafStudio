# 🎨 macOS 终端完全美化指南

## 第一步：安装 Oh My Zsh

Oh My Zsh 是一个强大的 Zsh 配置框架，提供了丰富的主题和插件。

### 安装命令

```bash
# 安装 Oh My Zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

安装完成后，你的提示符会立即变得更好看！

---

## 第二步：安装 Powerlevel10k 主题（最强主题）

Powerlevel10k 是目前最流行、最强大的 Zsh 主题。

### 安装步骤

```bash
# 1. 克隆 Powerlevel10k 仓库
git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k

# 2. 编辑 ~/.zshrc 文件
nano ~/.zshrc

# 3. 找到 ZSH_THEME="robbyrussell" 这一行
# 改为：ZSH_THEME="powerlevel10k/powerlevel10k"

# 4. 保存并退出（Ctrl+O, Enter, Ctrl+X）

# 5. 重新加载配置
source ~/.zshrc
```

### 配置 Powerlevel10k

首次运行时会自动启动配置向导，如果没有启动，运行：

```bash
p10k configure
```

配置建议：
- ✅ **安装推荐字体**（选择 Yes）
- ✅ **选择 Rainbow 样式**（多彩图标）
- ✅ **显示时间**
- ✅ **显示 Git 状态**
- ✅ **双行提示符**（更清晰）

---

## 第三步：安装 Nerd Fonts（图标字体）

Nerd Fonts 包含了大量编程图标，让终端显示各种漂亮的符号。

### 推荐字体

```bash
# 使用 Homebrew 安装（推荐）
brew tap homebrew/cask-fonts

# 安装几个流行的等宽字体
brew install --cask font-fira-code-nerd-font
brew install --cask font-hack-nerd-font
brew install --cask font-jetbrains-mono-nerd-font
brew install --cask font-meslo-lg-nerd-font
```

### 在终端中设置字体

1. **Terminal.app**:
   - `Terminal` → `Preferences` → `Profiles`
   - 选择你的主题
   - 点击 `Font` 旁边的 `Change` 按钮
   - 选择 **"MesloLGS NF"** 或 **"JetBrainsMono Nerd Font"**
   - 字号建议：**13-14pt**

2. **iTerm2**:
   - `iTerm2` → `Preferences` → `Profiles` → `Text`
   - Font: 选择 **"MesloLGS NF"** 或其他 Nerd Font
   - 字号：**13-14**
   - 勾选 `Use ligatures`（连字效果）

---

## 第四步：安装有用的插件

编辑 `~/.zshrc`，找到 `plugins=()` 这一行：

```bash
nano ~/.zshrc
```

修改为：

```bash
plugins=(
  git                        # Git 命令补全和别名
  zsh-autosuggestions       # 自动建议（根据历史）
  zsh-syntax-highlighting   # 语法高亮
  colored-man-pages         # 彩色的 man 手册
  extract                   # 智能解压缩
  z                         # 快速跳转目录
  docker                    # Docker 补全
  kubectl                   # Kubernetes 补全
)
```

### 安装额外的插件

```bash
# zsh-autosuggestions（自动建议）
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions

# zsh-syntax-highlighting（语法高亮）
git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting

# 重新加载配置
source ~/.zshrc
```

---

## 第五步：iTerm2 配置（推荐）

如果你使用 iTerm2（比默认 Terminal 功能更强大）：

### 安装 iTerm2

```bash
brew install --cask iterm2
```

### iTerm2 推荐设置

#### 1. 透明度和模糊

- `iTerm2` → `Preferences` → `Profiles` → `Window`
- **Transparency**: 设置为 `10-15`（85-90%不透明）
- **Blur**: 设置为 `15-20`
- **Style**: 选择 `Normal` 或 `Full Screen`

#### 2. 配色方案

**选项A：使用内置主题**
- `Preferences` → `Profiles` → `Colors`
- `Color Presets...` → 选择：
  - **Solarized Dark**（经典暗色）
  - **Tango Dark**（舒适暗色）
  - **Pastel (Dark Background)**（柔和暗色）

**选项B：安装流行主题**

```bash
# 克隆 iTerm2 颜色方案仓库
git clone https://github.com/mbadolato/iTerm2-Color-Schemes.git ~/Downloads/iTerm2-Color-Schemes

# 在 iTerm2 中导入：
# Preferences → Profiles → Colors → Color Presets... → Import...
# 选择 ~/Downloads/iTerm2-Color-Schemes/schemes/ 中的 .itermcolors 文件
```

推荐主题：
- **Dracula**（紫色系，非常流行）
- **Nord**（蓝灰色系，舒适）
- **Gruvbox Dark**（复古暖色）
- **One Dark**（类似 VSCode 暗色主题）
- **Tokyo Night**（现代暗色）

#### 3. 热键窗口（超快捷）

- `Preferences` → `Keys` → `Hotkey`
- 勾选 `Show/hide all windows with a system-wide hotkey`
- 设置快捷键（推荐：`Option + Space` 或 `Cmd + \``）
- 这样你可以随时随地一键呼出终端！

---

## 第六步：Terminal.app 高级配置

如果你使用默认的 Terminal.app：

### 创建自定义主题

1. **打开终端偏好设置**
   ```
   Terminal → Preferences → Profiles
   ```

2. **基于 Pro 主题创建**
   - 选择 `Pro` 主题
   - 点击左下角的齿轮图标
   - 选择 `Duplicate Profile`
   - 命名为 "My Dark Theme"

3. **自定义颜色**

   **背景设置：**
   - Background Color: `#1a1a1a`（深灰黑）
   - Opacity: `90%`
   - ✅ Blur（勾选）

   **文字设置：**
   - Text: `#00ff00`（经典绿色）或 `#f8f8f2`（柔和白色）
   - Bold Text: `#00ff7f`（亮绿）或 `#ffffff`（纯白）
   - Selection: `#44475a`

   **ANSI Colors 配置：**（用于语法高亮）
   ```
   Black:      #282a36
   Red:        #ff5555
   Green:      #50fa7b
   Yellow:     #f1fa8c
   Blue:       #bd93f9
   Magenta:    #ff79c6
   Cyan:       #8be9fd
   White:      #f8f8f2
   ```

4. **字体设置**
   - Font: MesloLGS NF 或 JetBrains Mono Nerd Font
   - Size: 13pt
   - Line Spacing: 1.1

5. **光标设置**
   - Cursor: `Vertical Bar` 或 `Underline`
   - ✅ Blink cursor（闪烁光标）

6. **设为默认**
   - 点击底部的 `Default` 按钮

---

## 第七步：添加实用别名和函数

编辑 `~/.zshrc`，在文件末尾添加：

```bash
# 快捷命令别名
alias ll='ls -lah'                    # 详细列表
alias l='ls -la'                      # 显示所有文件
alias ..='cd ..'                      # 快速返回上级
alias ...='cd ../..'                  # 返回上上级
alias ....='cd ../../..'              # 返回上上上级

# Git 快捷命令
alias gs='git status'
alias ga='git add'
alias gc='git commit -m'
alias gp='git push'
alias gl='git pull'
alias gco='git checkout'
alias gb='git branch'
alias glog='git log --oneline --graph --decorate'

# 开发相关
alias python='python3'
alias pip='pip3'

# 清理终端
alias c='clear'
alias cls='clear'

# 快速编辑配置文件
alias zshconfig='nano ~/.zshrc'
alias zshreload='source ~/.zshrc'

# 显示路径
alias path='echo $PATH | tr ":" "\n"'

# 快速查看文件
alias cat='bat'  # 需要安装 bat: brew install bat

# 系统信息
alias myip='curl ifconfig.me'        # 显示公网IP
```

保存后运行：

```bash
source ~/.zshrc
```

---

## 第八步：安装实用 CLI 工具

这些工具能让终端体验更好：

```bash
# 1. bat - 更好的 cat（带语法高亮）
brew install bat

# 2. exa - 更好的 ls（带图标和颜色）
brew install exa
alias ls='exa --icons'
alias ll='exa -lah --icons'

# 3. fzf - 模糊搜索神器
brew install fzf
$(brew --prefix)/opt/fzf/install  # 安装快捷键绑定

# 4. htop - 更好的系统监控
brew install htop

# 5. tldr - 简化的命令帮助
brew install tldr

# 6. thefuck - 自动纠正命令
brew install thefuck
eval $(thefuck --alias)

# 7. ripgrep - 超快的搜索工具
brew install ripgrep

# 8. fd - 更好的 find
brew install fd

# 9. ncdu - 磁盘使用分析
brew install ncdu

# 10. neofetch - 显示系统信息（酷炫）
brew install neofetch
```

---

## 第九步：终端启动欢迎信息

在 `~/.zshrc` 末尾添加：

```bash
# 启动时显示系统信息（酷炫）
neofetch

# 或者显示一个简单的欢迎信息
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  Welcome back, $USER!"
echo "  $(date '+%Y-%m-%d %H:%M:%S')"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
```

---

## 🎨 推荐的完整配置组合

### 方案1：极简黑客风格
- **主题**: Powerlevel10k
- **配色**: 绿色文字 + 黑色背景
- **字体**: Fira Code Nerd Font
- **透明度**: 85%
- **特点**: 经典Matrix风格

### 方案2：现代开发者风格
- **主题**: Powerlevel10k
- **配色**: Dracula 或 One Dark
- **字体**: JetBrains Mono Nerd Font
- **透明度**: 90%
- **特点**: 舒适、高效

### 方案3：优雅简约风格
- **主题**: Powerlevel10k (Lean style)
- **配色**: Nord
- **字体**: Meslo Nerd Font
- **透明度**: 95%
- **特点**: 专业、干净

---

## 🚀 快速开始脚本

我为你准备了一个自动化安装脚本：

```bash
#!/bin/bash
# macOS 终端美化一键安装脚本

echo "🎨 开始美化你的终端..."

# 安装 Homebrew（如果还没有）
if ! command -v brew &> /dev/null; then
    echo "📦 安装 Homebrew..."
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
fi

# 安装 Oh My Zsh
echo "🔧 安装 Oh My Zsh..."
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended

# 安装 Powerlevel10k
echo "✨ 安装 Powerlevel10k 主题..."
git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k

# 安装字体
echo "🔤 安装 Nerd Fonts..."
brew tap homebrew/cask-fonts
brew install --cask font-meslo-lg-nerd-font
brew install --cask font-jetbrains-mono-nerd-font
brew install --cask font-fira-code-nerd-font

# 安装插件
echo "🔌 安装 Zsh 插件..."
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions
git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting

# 安装实用工具
echo "🛠️  安装实用工具..."
brew install bat exa fzf htop tldr ripgrep fd neofetch

# 备份原配置
cp ~/.zshrc ~/.zshrc.backup

# 更新 .zshrc
echo "📝 更新 .zshrc 配置..."
sed -i '' 's/ZSH_THEME="robbyrussell"/ZSH_THEME="powerlevel10k\/powerlevel10k"/' ~/.zshrc
sed -i '' 's/plugins=(git)/plugins=(git zsh-autosuggestions zsh-syntax-highlighting colored-man-pages extract z)/' ~/.zshrc

echo ""
echo "✅ 安装完成！"
echo ""
echo "下一步："
echo "1. 重启终端"
echo "2. 运行 'p10k configure' 配置 Powerlevel10k"
echo "3. 在终端偏好设置中将字体改为 'MesloLGS NF'"
echo ""
```

将此脚本保存为 `beautify_terminal.sh`，然后运行：

```bash
chmod +x beautify_terminal.sh
./beautify_terminal.sh
```

---

## 📸 效果预览

安装完成后，你的终端会有：

- ✨ **彩色图标** - Git分支、文件夹、文件类型等
- 🚀 **命令执行时间** - 显示每条命令耗时
- 📊 **系统状态** - 电池、时间、目录路径等
- 💡 **智能提示** - 根据历史命令自动建议
- 🎨 **语法高亮** - 命令是否正确，实时高亮
- 🌈 **多彩输出** - 不同文件类型不同颜色

---

## 🎯 最后的建议

1. **不要过度配置** - 保持简洁高效
2. **定期更新** - `omz update` 更新 Oh My Zsh
3. **学习快捷键** - 提高效率
4. **备份配置** - 定期备份 `~/.zshrc` 和 `~/.p10k.zsh`

享受你的新终端吧！🎉
