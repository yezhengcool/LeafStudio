# 🚀 终端美化快速参考

## 一键安装

```bash
cd /Users/mac/Desktop/LeafStudio
./beautify_terminal.sh
```

---

## 📋 安装内容

### 主要组件
- ✅ **Oh My Zsh** - Zsh 配置框架
- ✅ **Powerlevel10k** - 最强大的主题
- ✅ **Nerd Fonts** - 图标字体（4个字体家族）
- ✅ **Zsh 插件** - 自动建议、语法高亮等
- ✅ **CLI 工具** - bat, exa, fzf, htop 等 10 个工具
- ✅ **iTerm2**（可选） - 更强大的终端

---

## ⚙️ 手动配置步骤

### 1. Terminal.app 透明度设置

**步骤：**
1. `Terminal` → `Preferences...`（或 `Cmd + ,`）
2. 点击 `Profiles` 标签
3. 选择 `Pro` 主题
4. **Background 部分：**
   - Color: 黑色或深灰
   - **Opacity: 90%**（向左拖动滑块）
   - ✅ **勾选 Blur**
5. **Font 部分：**
   - 点击 `Change` 按钮
   - 选择 **MesloLGS NF**
   - 字号：**13pt**
6. 点击底部 `Default` 按钮设为默认
7. 关闭窗口，重新打开终端查看效果

### 2. iTerm2 透明度设置

**步骤：**
1. `iTerm2` → `Preferences...`
2. `Profiles` → `Window` 标签
3. **Transparency:** 拖动到 `10-15`（约85-90%不透明）
4. **Blur:** 拖动到 `15-20`
5. `Profiles` → `Text` 标签
6. **Font:** 选择 **MesloLGS NF, 13pt**
7. ✅ 勾选 `Use ligatures`

### 3. 配置 Powerlevel10k

**运行配置向导：**
```bash
p10k configure
```

**推荐选项：**
- Diamond → `y` (Yes)
- Lock → `y` (Yes)
- Debian → `y` (Yes)
- ✨ Prompt Style → `3` (Rainbow)
- Character Set → `1` (Unicode)
- Show time? → `3` (12-hour format)
- Separator → `1` (Angled)
- Heads → `1` (Sharp)
- Tails → `1` (Flat)
- Height → `2` (Two lines)
- Connection → `1` (Disconnected)
- Frame → `1` (No frame)
- Spacing → `1` (Compact)
- Icons → `2` (Many icons)
- Flow → `1` (Concise)
- Transient → `n` (No)
- Instant → `y` (Yes)

---

## 🎨 推荐主题配色

### 主题1：经典黑客风格（Matrix）
```
背景: #000000 (纯黑)
文字: #00FF00 (绿色)
透明度: 85%
字体: Fira Code Nerd Font, 13pt
```

### 主题2：现代开发者（Dracula）
```
背景: #282a36
文字: #f8f8f2
强调: #bd93f9 (紫色)
透明度: 90%
字体: JetBrains Mono Nerd Font, 13pt
```

### 主题3：优雅简约（Nord）
```
背景: #2e3440
文字: #eceff4
强调: #88c0d0 (蓝色)
透明度: 92%
字体: Meslo NF, 13pt
```

---

## 🔧 常用命令速查

### Oh My Zsh
```bash
omz update                    # 更新 Oh My Zsh
omz plugin list              # 查看已安装插件
omz theme list               # 查看可用主题
```

### Powerlevel10k
```bash
p10k configure               # 重新配置主题
p10k reload                  # 重新加载配置
nano ~/.p10k.zsh            # 编辑配置文件
```

### 配置管理
```bash
zshconfig                    # 编辑 ~/.zshrc
zshreload                    # 重新加载配置
source ~/.zshrc              # 重新加载配置
```

### 实用别名（已配置）
```bash
ll                           # 详细列表（带图标）
lt                           # 树形显示（2层）
..                           # 返回上级目录
...                          # 返回上上级
gs                           # git status
glog                         # git log 图形化
myip                         # 显示公网IP
ports                        # 查看监听端口
```

---

## 🛠️ 故障排除

### 字体显示异常（乱码）
**原因:** 未设置 Nerd Font  
**解决:** 
```
Terminal → Preferences → Profiles → Font → Change
选择: MesloLGS NF, 13pt
```

### Powerlevel10k 不显示
**原因:** 主题未在 .zshrc 中配置  
**解决:**
```bash
nano ~/.zshrc
# 找到 ZSH_THEME 行，改为：
ZSH_THEME="powerlevel10k/powerlevel10k"
# 保存后：
source ~/.zshrc
```

### 图标不显示
**原因:** 字体不支持图标  
**解决:**
```bash
# 重新安装字体
brew install --cask font-meslo-lg-nerd-font
# 在终端设置中选择 MesloLGS NF
```

### 插件不工作
**原因:** 插件未在 .zshrc 中启用  
**解决:**
```bash
nano ~/.zshrc
# 找到 plugins= 行，添加插件：
plugins=(
  git
  zsh-autosuggestions
  zsh-syntax-highlighting
  colored-man-pages
)
# 保存后：
source ~/.zshrc
```

### 自动建议颜色太亮
**解决:**
```bash
# 在 ~/.zshrc 末尾添加：
echo 'ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=#555555"' >> ~/.zshrc
source ~/.zshrc
```

---

## 📚 进阶配置

### 添加自定义插件
```bash
# 编辑 .zshrc
nano ~/.zshrc

# 修改 plugins 行
plugins=(
  git
  zsh-autosuggestions
  zsh-syntax-highlighting
  colored-man-pages
  extract                    # 智能解压
  z                          # 目录快速跳转
  docker                     # Docker 补全
  kubectl                    # Kubernetes 补全
  npm                        # NPM 补全
  node                       # Node.js 补全
)
```

### 自定义 Powerlevel10k 元素
```bash
# 编辑配置
nano ~/.p10k.zsh

# 常用元素：
# - dir               # 当前目录
# - vcs               # Git 状态
# - time              # 时间
# - battery           # 电池
# - ram               # 内存使用
# - load              # CPU 负载
# - disk_usage        # 磁盘使用
```

### 添加启动欢迎信息
```bash
# 在 ~/.zshrc 末尾添加：
neofetch                     # 显示系统信息
# 或
echo "Welcome, $USER! 🚀"
```

---

## 📖 推荐资源

- **Oh My Zsh 官网:** https://ohmyz.sh/
- **Powerlevel10k 文档:** https://github.com/romkatv/powerlevel10k
- **Nerd Fonts 官网:** https://www.nerdfonts.com/
- **iTerm2 官网:** https://iterm2.com/
- **配色方案:** https://iterm2colorschemes.com/

---

## 💡 小贴士

1. **定期更新:** 运行 `omz update` 保持 Oh My Zsh 最新
2. **备份配置:** 修改前先备份 `~/.zshrc`
3. **逐步启用插件:** 不要一次性启用太多插件，可能会变慢
4. **快捷键:**
   - `Ctrl + R` - 搜索历史命令（fzf增强）
   - `Ctrl + T` - 模糊搜索文件
   - `Alt + C` - 模糊搜索并跳转目录
5. **性能优化:** 如果终端启动慢，运行 `p10k configure` 选择 Instant Prompt

---

**享受你的终端！** 🎉
